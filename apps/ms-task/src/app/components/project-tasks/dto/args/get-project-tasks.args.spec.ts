import { GetProjectTasksArgs } from './get-project-tasks.args';

describe('GetProjectTasksArgs', () => {
  it('should be defined', () => {
    expect(new GetProjectTasksArgs()).toBeDefined();
  });
});
